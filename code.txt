model {

          # Evidence synthesis on effectiveness of antibiotics prophylaxis vs # status quo
	r1 ~ dbin(pi1, n1)
	r2 ~ dbin(pi2, n2)
	logit(pi1) <- alpha
	logit(pi2) <- alpha + delta
	delta ~ dnorm(mean.delta, tau.delta)
	alpha ~ dnorm(mean.alpha, prec.alpha)

	# Length of stay in hospital
	l.no~ dlnorm(mu.l.no, tau.l.no)
	l.comp~ dlnorm(mu.l.comp, tau.l.comp)

	# Informative prior of probability of complications without antibiotics prophylaxis
        	p1~ dbeta(a.p1, b.p1)

	# Odds Ratio of complications under treatment with antibiotics prophylaxis
          rho <- exp(delta)

	# Informative prior of probability of complications under the treatment of antibiotics prophylaxis
	p2 <- (rho*p1/(1-p1))/(1+rho*p1/(1-p1))
} 